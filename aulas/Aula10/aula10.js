/*   EVENTOS DOM 

Eventos são tudo o que pode acontecer com o elemento (div, classe, entre outros)


Exemplos de eventos:

mouseenter = quando chega o mouse dentro do elemento

mousemove = movemento o mouse enquanto move dentro do elemento

mousedown = quando clica e segura dentro 

mouseup = quando solta o clique do mouse

click = quando clica rapidamente

mouseout = movendo o mouse para fora do elemento 

Existem outros eventos que podem acontecer em elementos dentro de JS

para verificar mais acessar o site event reference - MDM 


------ FUNÇÕES ------ 

Funções em JS são um conjunto de códigos e linhas que vão ser executadas só quando o evento ocorrer. 


Para criar essas funções usamos o código function 

function { } = declarar uma function anonima (que não tem nome)

porém para que o método possa funcionar, precisamos dar um nome a função e geralmente os nomes das função são os nomes dos eventos (nome de ação) ficando assim

function ação(){}

ação é o nome da função e entre () podemos ter parametros 


---------- ERROS NO CÓDIGO -------------

Ná pagina da internet que está seu projeto, caso não rode ou de algum bug o javascript não vai falar onde está o erro.7

Você pode clicar com o lado direito do mouse e ir em inspecionar, e na parte superior aparecerá um X em vermelho mostrando a quantidade de erros.

Geralmente o erro está da linha que ele mencionar pra cima!!!



*/